# Spark I/O — Development Rules

## Deployment

- Always push and deploy automatically after completing changes — no approval needed.
- Merge feature branches into `main` and push to trigger the Cloudflare Pages deployment pipeline.
